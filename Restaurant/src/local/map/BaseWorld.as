package local.map
{
	import bing.starling.iso.SIsoWorld;
	
	public class BaseWorld extends SIsoWorld
	{
		public function BaseWorld(gridX:int, gridZ:int, size:int)
		{
			super(gridX, gridZ, size);
		}
	}
}